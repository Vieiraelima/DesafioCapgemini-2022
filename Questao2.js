// Dado um vetor de inteiros n e um inteiro qualquer x. 
// Construa um algoritmo que determine o número de elementos pares do vetor que
// tem uma diferença igual ao valor de x.

//Explicação:
//Existem 3 pares de inteiros no vetor com uma diferença de 2: [5, 3], [4, 2] e [3, 1].
//se (num % 2 = 0 ) entao


 const array = [1, 5, 3, 4, 2];
 const x = 2
 console.log ([elemento],array.indexOf(0));

 function elemento (array) {
    const soma = x + (array.indexOf(0));
    for ( )
// Não consigo resolver

